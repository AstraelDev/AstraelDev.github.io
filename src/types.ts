export type TimePhase = 'aube' | 'jour' | 'crepuscule' | 'nuit';

export interface ThemeColors {
  bg: string;
  accent: string;
  text: string;
  secondary: string;
  clouds: string[];
  celestial: string;
}
